from django.test import TestCase

# Create your tests here.
"""
class TestBook(TestCase):

    def test_book(self):
        response = self.client.get('/book/')
        self.assertEqual(response.status_code, 200)

    def test_equal(self):
        response = self.client.get('/book/equal/')
        self.assertEqual(response.status_code, 200)

    def test_is_cheap(self):
        response = self.client.get('/book/is_cheap_or_expensive/')
        self.assertEqual(response.status_code, 200)

    def test_five_hundred(self):
        response = self.client.get('/book/five_hundred/')
        self.assertEqual(response.status_code, 200)

    def test_sales_income(self):
        response = self.client.get('/book/sales_income/')
        self.assertEqual(response.status_code, 200)

    def test_prefix(self):
        response = self.client.get('/book/prefix/')
        self.assertEqual(response.status_code, 200)

"""